'use strict';

describe('myApp.curric module', function() {

  beforeEach(module('myApp.curric'));

  describe('curric controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var curricCtrl = $controller('CurricCtrl');
      expect(curricCtrl).toBeDefined();
    }));

  });
});
